{
  "name": "perfin",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --parallel dev",
    "build": "bun run --parallel build"
  },
  "overrides": {
    "lightningcss": "1.30.1"
  }
}
